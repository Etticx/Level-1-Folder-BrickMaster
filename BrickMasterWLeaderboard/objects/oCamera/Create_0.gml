// Get the camera ID
var camera_id = view_camera[0];

// Get the camera's width and height
wCam = camera_get_view_width(camera_id);
hCam = camera_get_view_height(camera_id);


window_set_fullscreen(true);