r_pressed = false;

// Set fullscreen mode
window_set_fullscreen(true);

// Retrieve the time taken from the global variable
completion_time = global.timer_value + 1; // Use the global variable instead